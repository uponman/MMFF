# YOLOv5 🚀 by Ultralytics, AGPL-3.0 license

# Parameters
nc: 4  # number of classes
depth_multiple: 0.33  # model depth multiple
width_multiple: 0.5  # layer channel multiple
backbonedn: 7
anchors:
  - [10,13, 16,30, 33,23]  # P3/8
  - [30,61, 62,45, 59,119]  # P4/16
  - [116,90, 156,198, 373,326]  # P5/32

# YOLOv5 v6.0 backbone
backboned:
  # input 3*640*640
  # [from, number, module, args]
  [[-1, 1, Conv, [64, 6, 2, 2]],  # 0-P1/2     args[ch_in, ch_out, kernel, stride, padding, groups]
   # 64*320*320 
   [-1, 1, Conv, [128, 3, 2]],  # 1
   # 128*160*160
   [-1, 3, C3, [128]],  # 2
   # 128*160*160
   [-1, 1, Conv, [256, 3, 2]],  # 3
   # 256*80*80
   [-1, 6, C3, [256]],  # 4
   # 256*80*80
   [-1, 1, Conv, [512, 3, 2]],  # 5
   # 512*40*40
   [-1, 3, C3, [512]],  # 6
   # 512*40*40
  ]

backbone:
  # [from, number, module, args]
  [[-1, 1, Conv, [64, 6, 2, 2]],  # 7-P1/2
   # 64*320*320
   [-1, 1, Conv, [128, 3, 2]],  # 8
   # 128*160*160
   [-1, 3, C3, [128]],  # 9
   # 128*160*160
   [-1, 1, Conv, [256, 3, 2]],  # 10
   # 256*80*80
   [-1, 6, C3, [256]],  # 11
   # 256*80*80
   [-1, 1, Conv, [512, 3, 2]],  # 12
   # 512*40*40
   [-1, 3, C3, [512]],  # 13
   # 512*40*40
   [[-1, 6], 1, ConcatSc, [256, 512, 256]],  # 14
   # 1024*40*40
   [-1, 1, Conv, [1024, 3, 2]],  # 15
   # 1024*20*20
   [-1, 9, C3, [1024]],  # 16
   # 1024*20*20
   [-1, 1, SPPF, [1024, 5]],  # 17
   # 1024*20*20
  ]

# YOLOv5 v6.0 head
head:
  [[-1, 1, Conv, [512, 1, 1]],  # 18
   # 512*20*20
   [-1, 1, nn.Upsample, [None, 2, 'nearest']],  # 19
   # 512*40*40
   [[-1, 14], 1, Concat, [1]],  # cat backbone P4  20
   # 1536*40*40
   [-1, 1, C3, [512, False]],  # 21
   # 512*40*40

   [-1, 1, Conv, [256, 1, 1]],  # 22
   # 256*40*40
   [-1, 1, nn.Upsample, [None, 2, 'nearest']],  # 23
   # 256*80*80
   [[-1, 11], 1, Concat, [1]],  # cat backbone P3 24
   # 512*80*80
   [-1, 1, C3, [256, False]],  # 25 (P3/8-small)
   # 256*80*80

   [-1, 1, Conv, [256, 3, 2]],  # 26
   # 256*40*40
   [[-1, 22], 1, Concat, [1]],  # cat head P4  27
   # 512*40*40
   [-1, 1, C3, [512, False]],  # 28 (P4/16-medium)
   # 512*40*40

   [-1, 1, Conv, [512, 3, 2]],  # 29
   # 512*20*20
   [[-1, 18], 1, Concat, [1]],  # cat head P5  30
   # 1024*20*20
   [-1, 1, C3, [1024, False]],  # 31 (P5/32-large)
   # 1024*20*20

   [[25, 28, 31], 1, Detect, [nc, anchors]],  # Detect(P3, P4, P5)  32
  ]